# SPEC優先開發規則

- 本專案以可持續記錄與狀態變化之活性SPEC文檔做為開發骨幹，一切行動皆源於SPEC需求。

## 每次工作先做什麼

- 先讀doc資料夾內的spec文檔，依照該次任務範圍grep相關條目，不隨意全文讀入，除非當前任務確實尋找不到指導時才全文讀入。
- 修改前執行 `git status --short`，不要覆蓋其他未完成工作。
- 本環境為git控制專案，每次修改都必須有commit，視修改範圍需要自行決定是否要另開branch，若只是小修補就保持在main裏頭直接作業。
- 本專案會使用GitHub雲端倉庫，pull或push都為同一位置，除非用戶下指令要求，否則不得自行執行任何雲端倉庫操作。
- 進行merge前檢查是否存在conflict，若不是功能性的矛盾就自行處理掉，在任務完成時報告用戶即可，若產生功能性矛盾就立即停止作業，向用戶報告和提出建議。

## SPEC使用規則
- 本專案spec應是活的，內容要隨開發不斷修改，每次做出功能調整應紀錄決策原因，只寫重點，不寫些枝微末節的細節。
- README檔案是向使用者解釋程式功能與使用方式的文件，不是設計和需求的說明書，修改時應分清他和spec的差別，若該次修改與功能無關README就不需紀錄。
- 優先用檔名、目錄與 grep 搜尋，再精讀少量文件。
- Tool output 很長時只保留必要段落；不要把完整 build log 寫入 spec。
- 移除失效文件，避免後續 agent 同時看到互相矛盾的規則。